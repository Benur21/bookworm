name: action-to-publish-app
on: [push]
jobs:
  do-my-thing-xD:
    runs-on: ubuntu-latest
    steps:
      - run: help
      - run: |
              pwd
              ls -l -a
              cd ..
              pwd
              ls -l -a
              yarn run deploy
