name: action-to-publish-app
on: [push]
jobs:
  do-my-thing-xD:
    runs-on: ubuntu-latest
    steps:
      - run: help
      - run: git branch
      - run: echo "$(ls -l -a)"
      - run: ls -l -a
      - run: cd ..
      - run: ls
      - run: pwd
      - run: yarn run deploy
